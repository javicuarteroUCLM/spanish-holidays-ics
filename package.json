{
  "name": "spanish-holidays-ics",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check:generated": "npm run generate && git diff --exit-code -- calendars data/normalized/2026",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "generate": "npm run build --silent && node dist/cli.js generate",
    "lint": "eslint .",
    "sources:check": "node scripts/refresh-sources.mjs --check",
    "sources:refresh": "node scripts/refresh-sources.mjs --write",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "validate": "npm run build --silent && node dist/cli.js validate",
    "verify": "npm run format:check && npm run lint && npm run typecheck && npm test && npm run generate && npm run validate"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^24.3.0",
    "eslint": "^10.10.0",
    "globals": "^16.3.0",
    "prettier": "^3.9.6",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.69.0",
    "vitest": "^5.0.0"
  },
  "dependencies": {
    "ical.js": "^2.2.1"
  }
}
